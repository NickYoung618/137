# Implementation Plan: 孔2最新唯一真值边缘语义重构

## Scope

只修改孔2 current-capture 适配层、版本化配置、离线工具、测试、契约和规格。复用
`algorithms/hole_2/main.py` v6，不修改该文件。

## Phases

1. 冻结最新外置资产哈希并复现无真值运行基线。
2. 增加只在离线运行的叠加图/逐点/边缘剖面诊断工具，形成根因证据。
3. 增加尺寸7成对极性轮廓中心候选与质量字段，失败仍走严格 v6 回退。
4. 增加 Phi 参考相位、明确正负极性和稳健多圆精配准；保持所有原恢复边界。
5. 更新真实 E2E 哈希/门限，增加无绝对路径的一键单图验收脚本和契约测试。
6. 运行9帧外置对照、全套门禁、SpecKit analyze 和 Git 审计；分里程碑提交推送。

## Safety and compatibility

- 检测函数签名不增加 target annotation。
- 旧参考坐标业务列和目标坐标审计输出保持。
- 新候选失败显式失败；只有原 v6 质量有效才回退。
- 不以真值直径、长度、圆心、文件名或哈希参与候选评分。
- 图片、标注、JSONL、PNG和运行报告均写到工作树外。
