# Clarifications: 孔2最新唯一真值边缘语义重构

## 2026-08-15

1. **哪个标注是当前精度真值？** 仅 SHA-256 为
   `018e3449c051c15f7946315bd0d7f21cd79f4d4983efca0d11c7d98f02bfffa6`
   的 `端面标注样品.json`；旧审核 JSON 只能生成历史对照。
2. **80点是否是契约？** 否。当前文件实际为80点，运行/验收仍遵循不少于8个有限点、
   可拟圆且通过残差门的语义契约。
3. **检测可否读取当前真值？** 不可。真值仅由离线诊断/验收读取；运行时继续只读取旧参考
   annotation、旧参考 image、目标 image 和版本化 config。
4. **能否为了通过单图直接加长度或半径常数？** 不可。任何位移必须来自成对梯度、参考边缘
   相位、稳健几何拟合和既有质量门。
5. **旧审核单图若不再满足新定义怎么办？** 作为非阻断历史报告；不得反向放宽最新真值门。
