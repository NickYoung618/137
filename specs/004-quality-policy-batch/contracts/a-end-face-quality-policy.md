# Contract: `a-end-face-quality-policy/1`

The policy versions localization checks only. Core feature-measurement thresholds remain fixed by the core SHA-256.
The default `requiredFeatureLabels` is empty, so feature measurements do not veto localization. The root
machine-readable schema is `contracts/a-end-face-quality-policy.schema.json`.
