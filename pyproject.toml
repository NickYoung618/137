[project]
name = "housing-inspection-a-end-face"
version = "1.1.0"
description = "Independent quality-aware JSON CLI around the preserved desktop A-end-face detector"
requires-python = ">=3.12,<3.13"
dependencies = [
  "numpy==2.4.4",
  "Pillow==12.2.0",
]

[tool.uv]
package = false
