[project]
name = "housing-inspection-slot-pose"
version = "0.4.1"
description = "Offline dataset tooling and fail-closed slot pose contract skeleton"
requires-python = ">=3.12,<3.13"
dependencies = [
  "numpy==2.4.4",
  "Pillow==12.2.0",
]

[tool.uv]
package = false
