[project]
name = "housing-inspection-a-end-face"
version = "1.3.0"
description = "Quality-aware A-end-face JSON CLI with an independently gated short-line candidate"
requires-python = ">=3.12,<3.13"
dependencies = [
  "numpy==2.4.4",
  "Pillow==12.2.0",
]

[tool.uv]
package = false
